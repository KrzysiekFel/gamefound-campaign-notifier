services:

  app:
    build: .
    container_name: gamefound-app
    ports:
      - "8080:8080"
    depends_on:
      - redis

  redis:
    image: redis:7-alpine
    container_name: gamefound-redis
    ports:
      - "6379:6379"